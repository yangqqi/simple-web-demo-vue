import Vue from 'vue';
import { List, DatetimePicker, Popup, PullRefresh, Step, Steps } from 'vant';

Vue.use(List)
    .use(DatetimePicker)
    .use(Popup)
    .use(PullRefresh)
    .use(Step)
    .use(Steps);
